const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			"fontFamily": {
				'pirate': ['"Pirata One"', 'sans-serif'],
			},
		}
	},

	plugins: []
};

module.exports = config;
